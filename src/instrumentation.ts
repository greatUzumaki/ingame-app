export async function register() {
  // NODE_TLS_REJECT_UNAUTHORIZED=0 is set as an environment variable
  // (locally in .env.local, on Vercel in project settings).
  // No hardcoding needed here.
}
